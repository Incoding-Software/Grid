
@using Grid.Components
@using GridUI.Models
@using Incoding.MvcContrib


@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Styling";
}



<div class="container">
    <div class="row col-md-12" style="margin-top:50px;">

        <h1>GridCssStyling</h1>

        <div class="example">
            @(Html.Grid<ProductVm>()
                      .Id("GridCssStylingExample")
                      .Styling(BootstrapTable.Bordered | BootstrapTable.Hover)
                      .Columns(column =>
                      {
                          column.Bound(r => r.Name)
                              .Title("Product Name");

                          column.Bound(r => r.Price);

                          column.Bound(r => r.Date)
                              .Title("Date of made");
                      })
                      .AjaxGet(Url.Action("GetProducts", "Home")))
        </div>
        <div class="sourceCode">
            <pre>
                      Html.Grid<productvm>()
                      .Id("GridCssStylingExample")
                      .Styling(BootstrapTable.Bordered | BootstrapTable.Hover)
                      .Columns(column =>
                      {
                          column.Bound(r => r.Name)
                                .Title("Product Name");
                          column.Bound(r => r.Price);
                          column.Bound(r => r.Date)
                                .Title("Date of made");
                      })
                      .AjaxGet(Url.Action("GetProducts", "Home"))
                </pre>
        </div>
        
        <h1>HeaderClass and Class</h1>

        <div class="example">
            @(Html.Grid<ProductVm>()
                  .Id("NextRowHtmlAttrExample2")
                  .Styling(BootstrapTable.Bordered | BootstrapTable.Hover)
                  .Columns(column =>
                  {
                      column.Bound(r => r.Name)
                          .Title("Product Name")
                          .HeaderClass(B.Active | B.Disabled);

                      column.Bound(r => r.Price)
                          .Class(B.Container);

                      column.Bound(r => r.Date)
                          .Title("Date of made")
                          .Class("success");
                  })
                  .AjaxGet(Url.Action("GetProducts", "Home")))
        </div>
        <div class="sourceCode">
            <pre>
                      Html.Grid<ProductVm>()
                          .Id("NextRowHtmlAttrExample2")
                          .Styling(BootstrapTable.Bordered | BootstrapTable.Hover)
                          .Columns(column =>
                          {
                              column.Bound(r => r.Name)
                                  .Title("Product Name")
                                  .HeaderClass(B.Active | B.Disabled);

                              column.Bound(r => r.Price)
                                  .Class(B.Container);

                              column.Bound(r => r.Date)
                                  .Title("Date of made")
                                  .Class("success");
                          })
                          .AjaxGet(Url.Action("GetProducts", "Home"))
                </pre>
        </div>


        <h1>HeadHtmlAttr</h1>

        <div class="example">
            @(Html.Grid<ProductVm>()
                      .Id("HeadHtmlAttrExample")
                      .Styling(BootstrapTable.Hover)
                      .HeadHtmlAttr(BootstrapClass.Success)
                      .Columns(column =>
                      {
                          column.Bound(r => r.Name)
                              .Title("Product Name");

                          column.Bound(r => r.Price);

                          column.Bound(r => r.Date)
                              .Title("Date of made");
                      })
                      .AjaxGet(Url.Action("GetProducts", "Home")))
        </div>
        <div class="sourceCode">
            <pre>
                      Html.Grid<productvm>()
                      .Id("HeadHtmlAttrExample")
                      .Styling(BootstrapTable.Hover)
                      .HeadHtmlAttr(BootstrapClass.Success) //.HeadHtmlAttr("success")
                      .Columns(column =>
                      {
                          column.Bound(r => r.Name)
                                .Title("Product Name");
                          column.Bound(r => r.Price);
                          column.Bound(r => r.Date)
                                .Title("Date of made");
                      })
                      .AjaxGet(Url.Action("GetProducts", "Home"))
                </pre>
        </div>


        <h1>RowHtmlAttr</h1>

        <div class="example">
            @(Html.Grid<ProductVm>()
                      .Id("RowHtmlAttrExample")
                      .Styling(BootstrapTable.Hover)
                      .HeadHtmlAttr(BootstrapClass.Success)
                      .Columns(column =>
                      {
                          column.Bound(r => r.Name)
                              .Title("Product Name");

                          column.Bound(r => r.Price);

                          column.Bound(r => r.Date)
                              .Title("Date of made");
                      })
                      .AjaxGet(Url.Action("GetProducts", "Home"))
                      .RowHtmlAttr(BootstrapClass.Warning))
        </div>
        <div class="sourceCode">
            <pre>
                      Html.Grid<productvm>()
                      .Id("RowHtmlAttrExample")
                      .Styling(BootstrapTable.Hover)
                      .HeadHtmlAttr(BootstrapClass.Success)
                      .Columns(column =>
                      {
                          column.Bound(r => r.Name)
                                .Title("Product Name");
                          column.Bound(r => r.Price);
                          column.Bound(r => r.Date)
                                .Title("Date of made");
                      })
                      .AjaxGet(Url.Action("GetProducts", "Home"))
                      .RowHtmlAttr(BootstrapClass.Warning)
                </pre>
        </div>


        <h1>HeaderHtmlAttr</h1>

        <div class="example">
            @(Html.Grid<ProductVm>()
                      .Id("HeaderHtmlAttrExample")
                      .Styling(BootstrapTable.Hover)
                      .Columns(column =>
                      {
                          column.Bound(r => r.Name)
                              .Title("Product Name")
                              .HeaderHtmlAttr(BootstrapClass.Danger);

                          column.Bound(r => r.Price)
                              .HeaderHtmlAttr(BootstrapClass.Success);

                          column.Bound(r => r.Date)
                              .Title("Date of made")
                              .HeaderHtmlAttr(BootstrapClass.Warning);
                      })
                      .AjaxGet(Url.Action("GetProducts", "Home")))
        </div>
        <div class="sourceCode">
            <pre>
                      Html.Grid<productvm>()
                      .Id("HeaderHtmlAttrExample")
                      .Styling(BootstrapTable.Hover)
                      .Columns(column =>
                      {
                          column.Bound(r => r.Name)
                                .Title("Product Name")
                                .HeaderHtmlAttr(BootstrapClass.Danger);
                          column.Bound(r => r.Price)
                                .HeaderHtmlAttr(BootstrapClass.Success);
                          column.Bound(r => r.Date)
                                .Title("Date of made")
                                .HeaderHtmlAttr(BootstrapClass.Warning);
                      })
                      .AjaxGet(Url.Action("GetProducts", "Home"))
                </pre>
        </div>


        <h1>HtmlAttr</h1>

        <div class="example">
            @(Html.Grid<ProductVm>()
                      .Id("HtmlAttrExample")
                      .Styling(BootstrapTable.Hover)
                      .Columns(column =>
                      {
                          column.Bound(r => r.Name)
                              .Title("Product Name")
                              .HeaderHtmlAttr(BootstrapClass.Danger)
                              .HtmlAttr(BootstrapClass.Success);


                          column.Bound(r => r.Price)
                              .HeaderHtmlAttr(BootstrapClass.Success);

                          column.Bound(r => r.Date)
                              .Title("Date of made")
                              .HeaderHtmlAttr(BootstrapClass.Warning);
                      })
                      .AjaxGet(Url.Action("GetProducts", "Home")))
        </div>
        <div class="sourceCode">
            <pre>
                      Html.Grid<productvm>()
                      .Id("HtmlAttrExample")
                      .Styling(BootstrapTable.Hover)
                      .Columns(column =>
                      {
                          column.Bound(r => r.Name)
                                .Title("Product Name")
                                .HeaderHtmlAttr(BootstrapClass.Danger)
                                .HtmlAttr(BootstrapClass.Success);
                          
                          column.Bound(r => r.Price)
                                .HeaderHtmlAttr(BootstrapClass.Success);
                          column.Bound(r => r.Date)
                                .Title("Date of made")
                                .HeaderHtmlAttr(BootstrapClass.Warning);
                      })
                      .AjaxGet(Url.Action("GetProducts", "Home"))
                </pre>
        </div>
        
        
        <h1>NextRowHtmlAttr</h1>

        <div class="example">
            @(Html.Grid<ProductVm>()
                  .Id("NextRowHtmlAttrExample")
                  .Columns(column =>
                  {
                      column.Bound(r => r.Name)
                          .Title("Product Name");

                      column.Bound(r => r.Price);

                      column.Bound(r => r.Date)
                          .Title("Date of made");
                  })
                  .AjaxGet(Url.Action("GetProducts", "Home"))
                  .RowHtmlAttr(BootstrapClass.Success)
                  .NextRow(@<text>
                                <td style="width: 200px;"></td>
                                <td colspan="2">Notes: @item.ForRaw(r => r.Comment)</td>
                            </text>)
                  .NextRowHtmlAttr(BootstrapClass.Warning))
        </div>
        <div class="sourceCode">
            <pre>
            Html.Grid<ProductVm>()
                  .Id("NextRowHtmlAttrExample")
                  .Columns(column =>
                  {
                      column.Bound(r => r.Name)
                          .Title("Product Name");
                      column.Bound(r => r.Price);
                      column.Bound(r => r.Date)
                          .Title("Date of made");
                  })
                  .AjaxGet(Url.Action("GetProducts", "Home"))
                  .RowHtmlAttr(BootstrapClass.Success)
                  .NextRow(<text>
                            <td style="width: 200px;"></td>
                            <td colspan="2">Notes: item.ForRaw(r => r.Comment)</td>
                           </text>)
                  .NextRowHtmlAttr(BootstrapClass.Warning)
            </pre>
        </div>
    </div>
</div>
